let inputWidth;
let inputHeight;
inputWidth= +prompt("Nhap Do Dai: ");
inputHeight = +prompt("Nhap Do Rong: ");
let width = +parseInt(inputWidth);
let height = +parseInt(inputHeight);
let are = width * height;
document.write("This is Are: " + are);